# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

# Pytest Configuration for Mapping Tests

[pytest]
# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Directories to search for tests
testpaths = .

# Minimum Python version
minversion = 3.8

# Command line options
# --durations / --durations-min report the slowest tests on every run so the
# mapping test suite's performance is tracked and regressions stay visible.
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings
    --durations=25
    --durations-min=0.05

# Markers
markers =
    unit: Unit tests that test individual components in isolation
    integration: Integration tests that test the full pipeline
    slow: Tests that take a long time to run

# Coverage options
[coverage:run]
source = ../src
omit =
    */tests/*
    */test_*
    */__pycache__/*
    */venv/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod

# Output options
[coverage:html]
directory = htmlcov
